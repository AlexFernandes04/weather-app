import classes from "../styles/Header.module.css";

const header = () => {
    return(
        <div className={classes.Container}>
            Weather App
        </div>
    )
}

export default header;